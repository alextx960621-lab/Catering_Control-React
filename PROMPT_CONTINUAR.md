# Prompt de traspaso — Catering Control (React)

Pega este archivo completo como primer mensaje a la IA que va a continuar. Incluye contexto del proyecto, lo que ya está resuelto, y una lista puntual de lo que falta con pistas concretas de dónde mirar.

---

## ⚠️ REGLA FIJA para quien trabaje en este proyecto (incluida la próxima IA)

**Cada vez que se entreguen cambios de código, hay que devolver SIEMPRE estos 2 archivos juntos, sin excepción:**

1. **El proyecto completo comprimido en un .zip** (`catering-control-react-vNN.zip`, subiendo el número de versión), con todos los cambios ya aplicados.
2. **Este mismo archivo (`PROMPT_CONTINUAR.md`) actualizado**, moviendo lo recién resuelto a la sección "Ya resuelto" (con detalle de causa y archivos tocados, igual que las entradas de abajo) y dejando la sección "Pendiente" al día — para que la siguiente IA (o la misma, en otra sesión) pueda seguir exactamente desde donde quedó, sin tener que releer todo el historial de chat ni adivinar qué se hizo.

No entregar solo el zip, ni solo un resumen en el chat sin el zip, ni un prompt actualizado sin el zip correspondiente: los dos archivos van siempre en la misma entrega. Si en una entrega puntual no se tocó código (por ejemplo, solo se respondió una pregunta), no hace falta regenerar nada — la regla aplica cuando hay cambios de código de por medio.

**Si una entrega necesita un cambio en la base de datos** (tabla nueva, columna nueva, función RPC nueva o modificada, policy nueva, etc.), eso NUNCA queda solo en el código o solo en la cabeza de quien lo hizo — van SIEMPRE los dos:

1. El SQL correspondiente, agregado como sección nueva (numerada, con comentario explicando qué hace y por qué) al final de `supabase-setup-final-v2.sql` — ese archivo es el único que hay que correr para dejar un proyecto de Supabase nuevo al día, así que cualquier cambio de base de datos tiene que quedar reflejado ahí, sea o no la primera vez que se corre.
2. Una mención explícita EN ESTE ARCHIVO (en "Ya resuelto" si ya se aplicó, o en "Pendiente" si falta correrlo) diciendo qué sección del SQL hay que correr en el SQL Editor de Supabase y qué requiere del lado del código para que sirva de algo — para que quien continúe no tenga que adivinar si el código ya tiene su contraparte de base de datos corrida o no.

No alcanza con "ya lo agregué al SQL": si el archivo SQL cambió, el propio ZIP de esta entrega tiene que incluir la versión actualizada de `supabase-setup-final-v2.sql` (no alcanza con haberlo mandado en el chat aparte).

---

## Contexto del proyecto

Es una PWA de gestión de catering (React + Vite + Supabase) con dos frentes:
- **Panel de staff/drivers** (`/src/pages/PanelPage.jsx` y `/src/components/panel/**`): Día de trabajo (Despacho), Notas, Clientes, Drivers, Rutas, Planes, Sueldos, Inventario, Métricas, Usuarios, Configuración.
- **Portal del cliente** (`/src/pages/ClientePage.jsx` y `/src/components/cliente/**`): ver plan, pausar/reanudar servicio, pedir renovación o cambio de plan, notas/soporte.

El backend es Supabase: la mayoría de operaciones de guardado pasan por RPCs (`services/db.js`, `services/supabaseClient.js`), no acceso directo a tablas desde el cliente. Todas las tablas `db_*` tienen RLS `using (false)`: todo pasa por RPCs `security definer` que validan un token de sesión contra `db_sessions` (ver session token en `supabaseClient.js`). El SQL de arranque/al-día de un proyecto de Supabase es **un solo archivo, `supabase-setup-final-v2.sql`** (reemplaza a la cadena vieja de `supabase-setup-completo.sql` + `supabase-storage-setup.sql` + `supabase-security-lockdown.sql` + migraciones sueltas) — es seguro volver a correrlo entero en un proyecto que ya lo corrió antes (todo usa `IF NOT EXISTS`/`OR REPLACE`/`DROP POLICY IF EXISTS`). Cualquier cambio de base de datos nuevo se agrega ahí como sección nueva al final, nunca como un archivo `.sql` suelto aparte (ver la regla fija de arriba).

## Ya resuelto (no volver a tocar salvo que se reporte que sigue roto)

1. **Sueldos cuenta a todos los clientes, no solo Activos** — ya arreglado con un `payrollSnapshot` que se congela en `processDay()` (`DispatchPage.jsx`) y se usa en `PayrollPage.jsx` en vez de recalcular en vivo. Confirmado que coincide con la lógica que pidió el dueño del proyecto: día sin procesar = "—" (0), al procesar se congela la cantidad de carreras por driver, y edición posterior de un cliente NO mueve el historial ya cerrado.
2. **Tarifa (Bs/carrera) no respetaba cambios a mitad de mes** — `PayrollPage.jsx` multiplicaba TODO el mes por la tarifa de HOY. Arreglado: cada día se multiplica por la tarifa que tenía guardada ESE día (`rateForDate`).
3. **Al cambiar de día en Despacho, los campos editables (orden, notas, teléfonos, maps, fecha de retorno) se quedaban con el valor del día anterior** — las filas usaban `key={c.id}` sin la fecha, y los inputs son no controlados (`defaultValue`). Arreglado con `key={`${c.id}::${date}`}`.
4. **Día procesado no dejaba cambiar de fecha** (se quedaba pegado viendo el historial) — el branch de "día ya procesado" no tenía selector de fecha. Ya se agregó.
5. **Comprobante de pago en Notas** — antes aparecía como texto plano con URL larga; ahora se detecta y muestra como link "📎 Ver comprobante".
6. **Botones "Editar cliente" / "Renovar" en las tarjetas de Notas** — ya existen y funcionan, navegan a Clientes y abren el modal correspondiente para ese cliente (`pendingClientAction` en `PanelPage.jsx` → `ClientsPage.jsx`).
7. **Confirmación por WhatsApp al marcar "Cumplida" una nota de solicitud de plan** — implementado. Al confirmar una renovación desde el botón "Renovar" de una nota, se guarda el plan/días reales aplicados (`onRenewalCompleted` → `renewalByClient` en `PanelPage.jsx`), y al marcar la nota como cumplida se abre WhatsApp con el mensaje ya armado (`NotesPage.jsx`, funciones `isPlanRequestNote`, `renewalWaMessage`, `markDone`). Solo se dispara si el staff pasó por "Renovar"; si edita al cliente a mano y marca cumplida sin pasar por ahí, no hay mensaje automático (no hay forma de saber con certeza qué se confirmó). **(NUEVO) Bug real encontrado y corregido**: después de confirmar la renovación (o de guardar una edición) desde una nota, la app dejaba al staff en la pantalla de **Clientes** en vez de devolverlo a **Notas** — así que tenía que acordarse de volver solo y recién ahí tocar "Cumplida"; si no lo hacía, el WhatsApp nunca se disparaba. Se agregó una bandera `fromNote` en `ClientsPage.jsx` (se marca cuando el modal se abrió por `pendingClientAction`, se limpia al cancelar) y un nuevo prop `onReturnToNotes` (pasado desde `PanelPage.jsx` como `() => onNavigate('notes')`) que se llama automáticamente al terminar de guardar/renovar, devolviendo al staff a Notas listo para marcar "Cumplida". Archivos tocados: `ClientsPage.jsx`, `PanelPage.jsx`.
8. **PWA no se podía instalar / no actualizaba sola** — la causa real de "no instala" era `Vercel Deployment Protection (SSO)` bloqueando el `manifest.json` en el dominio de preview (eso es config de Vercel, no del código — hay que desactivarlo en el proyecto de Vercel o usar el dominio de producción). Además se agregó actualización automática y silenciosa del service worker (`registerType: 'autoUpdate'` en `vite.config.js` + registro manual en `main.jsx` con chequeo cada hora).
9. **Modal de columnas (⚙️ Columnas en Día de trabajo) aparecía debajo de la tabla en vez de como overlay encima** — ya se arregló usando `<dialog>` nativo con `showModal()`/`close()` en vez del atributo `open` puesto a mano (`ColumnsModal.jsx`).
10. **Formularios del portal de cliente de tamaño inconsistente** — `PauseForm`, `ResumeForm`, `ScheduledResumeCard` no tenían el wrapper `col-12` que sí tenía `AddressCard`/`NoteCard`. Ya arreglado. Y también se hizo `col-12` a `PlanCard` y `SupportCard` (antes eran `col-lg-8`/`col-lg-4`, quedaban más chicos que el resto).
11. **Botones "Editar cliente"/"Renovar" de las notas con el mismo color** — ya diferenciados (`info` azul para Editar, `violet` para Renovar). Ver convención de colores más abajo.
12. **Ordenar columnas en la tabla de Día de trabajo (y Clientes) no respondía bien** — existía toda la lógica de sort (`toggleSort`, `sortedList`, `compareSortValues`) pero le faltaba el CSS del botón (`.th-sortable`, `.th-sort-btn`, `.th-sort-icon`). Se agregó CSS completo en `DispatchPage.css` (aplica globalmente porque Vite bundlea todo el CSS junto).
13. **Campanita de notas en el menú lateral no marcaba número** — `notesCount` estaba literalmente hardcodeado en `0` en `PanelPage.jsx`. Ahora cuenta `notes.filter(nt => nt.status !== 'cumplida').length`.
14. **En el login, el footer de "Términos y condiciones" aparecía al lado de la tarjeta en vez de debajo (en pantallas anchas)** — el contenedor `<main>` tenía `d-flex align-items-center` SIN `flex-column`. Arreglado agregando `flex-column justify-content-center`.
15. **Borrado real en base de datos al eliminar ruta/driver/usuario/cliente** — confirmado por código: clientes usan `dbDeleteClientRows` → RPC `staff_delete_client_rows` (DELETE real). Drivers/Rutas/Usuarios se guardan como array JSON en la fila "personal" vía `dbSetFields`/RPC `staff_set_fields`: al eliminar, se llama `saveDrivers/saveRoutes/saveStaffUsers` con el array YA FILTRADO, y esa función sobreescribe el campo completo — el registro eliminado deja de existir en la base de datos, no es un borrado solo visual. (No se pudo verificar el SQL de esas RPCs porque vive del lado de Supabase, fuera de este repo.)
16. **(NUEVO) Ícono de "ver contraseña" en el login se veía redondo** — causa real encontrada: `src/pages/ClientePage.css` tenía la regla `.btn:not(.btn-close):not(.btn-check) { border-radius: 999px !important; ... }` SIN ámbito (aplicaba a `.btn` global), y como todo el CSS del proyecto se bundlea junto sin CSS Modules, ese `!important` se filtraba a TODAS las páginas — entre otras, al botón de ver contraseña de `StaffForm.jsx`, que debería tener esquina recta por estar dentro de un `.input-group` de Bootstrap. Ya existía además una regla correctamente acotada `.portal .btn { border-radius: 999px; ... }` más abajo en el mismo archivo — la regla de arriba era una duplicada sin scope, de más. Se corrigió acotándola a `.portal .btn:not(.btn-close):not(.btn-check) { font-weight: 650; }` (el border-radius ya lo pone la regla de abajo). Archivo tocado: `src/pages/ClientePage.css`. **Falta solo confirmar visualmente que ya no se ve redondo en producción** (no se pudo correr `npm run dev` en este entorno).
17. **(NUEVO) Botón "Compartir esta app" en desktop parecía "no hacer nada"** — antes el único feedback era el cambio de ícono a un check por 2 segundos, muy sutil. Se agregó un texto flotante explícito junto al botón: "Link copiado ✅" si `navigator.clipboard.writeText` funcionó, o "No se pudo copiar el link" si falló (antes ese caso de error quedaba en silencio total). Archivos tocados: `src/components/login/ShareAppButton.jsx` (nuevo estado `status: 'idle'|'copied'|'error'`), `src/pages/LoginPage.css` (clases `.share-app-btn-toast` / `.share-app-btn-toast-error`).
18. **(NUEVO) PWA cacheaba el ícono de forma muy agresiva (CacheFirst puro)** — en `vite.config.js` los íconos (`public/icons/*.png`, sin hash de contenido en el nombre de archivo) usaban la misma regla `CacheFirst` de 30 días que CSS/JS. A diferencia de CSS/JS (que Vite renombra con hash en cada build, así que un cambio real siempre pide una URL nueva), un ícono nuevo subido con el MISMO nombre de archivo podía quedar cacheado viejo hasta por 30 días. Se separó en su propia regla `StaleWhileRevalidate` (sirve la copia cacheada al instante pero refresca en segundo plano para la próxima carga). También se sacó `manifest.json` de la regla genérica de estáticos y se le puso su propia regla `NetworkFirst` (archivo chico, no vale la pena arriesgar sacar uno viejo con branding desactualizado). Archivo tocado: `vite.config.js`. **No se pudo confirmar en vivo el efecto real** (requiere probar un rebuild + reemplazo de ícono en producción).
19. **(NUEVO) Diagnóstico agregado para "Conectados ahora" siempre en 0** — sigue sin resolverse de raíz (ver pendiente #1 más abajo, sigue abierto), pero se reemplazó el `catch` silencioso de `.track()` y se agregó manejo explícito de los estados `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED` del `.subscribe()`, ambos con `console.warn` describiendo hipótesis concretas (RLS de `realtime.messages` si el proyecto tiene "Realtime Authorization" activado, o Realtime no habilitado en el proyecto). Archivo tocado: `src/services/supabaseClient.js`. **Diagnóstico temporal**: hay que revisar la consola del navegador en producción para ver qué warning aparece (si aparece alguno) y de ahí sacar la causa real; una vez resuelto, quitar los `console.warn` marcados como TEMPORAL en el código.
20. **(NUEVO) Armonización de colores de botones en toda la app** — se llevó la convención de colores por función también al patrón compacto de tabla (`.icon-btn`), que antes quedaba todo en el mismo azul salvo `.icon-btn.delete`. Se agregaron las clases `.icon-btn.info`, `.icon-btn.success`, `.icon-btn.warning`, `.icon-btn.violet` en `PanelPage.css` (mismo tratamiento que ya tenía `.icon-btn.delete`: fondo transparente con tinte suave del color, sin el degradado/sombra de los botones grandes para no sobrecargar filas de tabla). Se agregó también la variable de tema `--panel-violet` (antes el morado estaba hardcodeado a `#8a5cf6` solo para el tema claro; ahora tiene su propio tono por tema, igual que `--panel-primary`/`--panel-accent`/`--panel-danger`/`--panel-warning`) y `--panel-warning-ink` (texto ámbar oscuro legible sobre el tinte de warning, separado del amarillo/naranja de fondo). Se aplicó la clase que corresponde por función en TODOS los botones de tabla que faltaban:
    - **Editar → `icon-btn info`**: `ClientsPage.jsx`, `DriversPage.jsx`, `RoutesPage.jsx`, `PlansPage.jsx` (planes y artículos), `UsersPage.jsx` (usuarios y roles), `InventoryPage.jsx` (productos, vínculos y movimientos), `DeliveryPage.jsx` (editar marca de entrega).
    - **Pausar/Activar → `icon-btn warning`**: `ClientsPage.jsx` (toggle de estado del cliente, mismo criterio que "Reprogramar/Reabrir").
    - **Renovar → `icon-btn violet`**: `ClientsPage.jsx` (ya estaba así en Notas, ahora también en la tabla de Clientes).
    - Los botones de eliminar (`icon-btn delete`, ya en rojo) y las flechas de reordenar columnas en `ColumnsModal.jsx` (utilitarias, sin significado de acción) se dejaron sin cambios a propósito.
    - Archivos tocados: `src/pages/PanelPage.css`, `src/components/panel/clients/ClientsPage.jsx`, `src/components/panel/drivers/DriversPage.jsx`, `src/components/panel/routes/RoutesPage.jsx`, `src/components/panel/plans/PlansPage.jsx`, `src/components/panel/users/UsersPage.jsx`, `src/components/panel/inventory/InventoryPage.jsx`, `src/components/panel/delivery/DeliveryPage.jsx`.
    - **Falta confirmar visualmente** que los tintes se vean bien en los 3 temas (sobre todo el tinte de `warning` en tema nocturno/bosque, y que el morado de `violet` contraste bien) — no se pudo probar en vivo en este entorno.
21. **(NUEVO) Tema y preferencias de tablas ahora viajan con la cuenta, no solo con el dispositivo** — resuelve el pendiente #7 de rondas anteriores. **Requiere haber corrido `supabase-setup-final-v2.sql` completo (o, si ya tenías la v1 corrida, solo las secciones 14 y 15) en el proyecto de Supabase** — sin eso, `staff_save_own_prefs` no existe y las llamadas van a fallar en silencio (quedan logueadas como error de RPC en consola, no rompen la app: el tema simplemente no se sincroniza y se queda como estaba, solo local).
    - **STAFF**: nuevo `src/services/userPrefs.js`, store único de `{ theme, columns: {grupo: {hidden, order, widths}} }` por usuario, con lectura/escritura SIEMPRE instantánea en localStorage (`catering-user-prefs-v2`) y sincronización en segundo plano (debounce de ~900ms) contra `staff_save_own_prefs`. Nunca empuja nada al servidor hasta que `OperationsContext` confirme (con éxito) qué había guardado — mismo criterio que el patrón `confirmed` que ya protegía drivers/rutas/planes contra el bug de sobreescritura documentado más arriba en este archivo (acá el riesgo es mucho menor porque el RPC solo toca la clave del propio usuario, nunca la de otro, pero se mantuvo el mismo cuidado).
    - `src/services/columnPrefs.js` quedó como un re-export fino de `userPrefs.js` (mismas funciones, misma firma) para no tener que tocar los ~10 archivos que ya lo importaban (`DataTable.jsx`, `DispatchPage.jsx`, `PlansPage.jsx`, `UsersPage.jsx`, `DeliveryPage.jsx`, `AuditPage.jsx`, `InventoryPage.jsx`, `DriversPage.jsx`, `ClientsPage.jsx`, `RoutesPage.jsx`).
    - `OperationsContext.jsx` ahora recibe `userId` (se lo pasa `PanelPage.jsx`) y pide también el campo `userPrefs` al cargar el bloque `personal`; al confirmarse, hidrata `userPrefs.js` con lo que haya para ese usuario. El tema propio (si el usuario ya eligió uno) tiene prioridad sobre `settings.theme` (que sigue siendo el default de empresa para quien nunca tocó nada).
    - `PanelPage.jsx`: el estado inicial de `theme` ahora lee el cache local (vía `readStaffSession()`, síncrono) en vez de arrancar siempre en `'light'`, para no pintar el tema equivocado un instante antes de que confirme el servidor.
    - `SettingsPage.jsx`: `handleThemeChange` antes solo escribía una clave de localStorage (`staff-theme-${userId}`) que **nunca se leía en ningún lado** (era código muerto) y dejaba el cambio real solo en el estado del componente (se perdía al recargar, siempre volvía al tema de empresa). Ahora llama a `setTheme()` de `userPrefs.js`, que sí persiste y sincroniza. Se actualizó también el texto de esa pantalla (decía "solo afecta a tu navegador", ya no es así).
    - **CLIENTES (portal)**: se suma el campo `client.uiTheme` (columna dentro de su propia fila, whitelisteada en `cliente_save_profile` — sección 15 del SQL) como la fuente de verdad del tema del cliente entre sus dispositivos. `ClientePage.jsx`: al cargar, si `client.uiTheme` existe se usa ese (si no, se cae al último tema visto en ESTE navegador, vía `clienteStorage.js`, igual que antes); al cambiar de tema desde el Portal, se guarda con `dbSaveOwnClientProfile(id, {uiTheme})` (sincroniza con la cuenta) Y también en el cache local del dispositivo (para que login/index sigan viéndose consistentes al instante, sin depender de la red).
    - Archivos tocados: `src/services/db.js` (nueva `dbSaveOwnPrefs`), `src/services/userPrefs.js` (nuevo), `src/services/columnPrefs.js` (reescrito como re-export), `src/context/OperationsContext.jsx`, `src/pages/PanelPage.jsx`, `src/components/panel/settings/SettingsPage.jsx`, `src/pages/ClientePage.jsx`, `src/services/clienteStorage.js` (solo comentario, sin cambio de comportamiento).
    - **No se pudo confirmar en vivo** (mismo motivo que el resto de esta ronda: no hay entorno para correr `npm run dev` contra un Supabase real en este sandbox) — sí se confirmó que compila sin errores (`npm install && npm run build` OK) y que el flujo de datos es coherente por lectura de código. Antes de dar esto por cerrado del todo: cambiar el tema en Configuración, refrescar la página y confirmar que se mantiene; abrir el Panel con el mismo usuario en otro navegador/perfil y confirmar que ya sale con el tema elegido; lo mismo para el tema del portal de cliente entre dos dispositivos. Las preferencias de columnas (orden/ocultas/anchos) todavía no tienen un botón propio para "restaurar default" — si algo queda en un estado raro durante la prueba, se limpia borrando la clave `catering-user-prefs-v2` de `localStorage` en ese navegador (o la fila `db_personal` con `id='userPrefs'` en Supabase, para limpiarlo de raíz en todos los dispositivos).
22. **(NUEVO) "Conectados ahora" — se aplicó el fix de código, no solo el diagnóstico**: el canal ahora se abre con `private: true` (antes no lo tenía), que es el modo recomendado por Supabase y el que hace que las policies de la sección 14 de `supabase-setup-final-v2.sql` (RLS sobre `realtime.messages` para el rol `anon`) realmente se apliquen — con el canal público, esas policies podían terminar siendo irrelevantes según cómo esté configurado "Allow public access" en el dashboard (una config de interfaz no confirmable desde acá). Archivo tocado: `src/services/supabaseClient.js` (`joinPresence()`). Sigue habiendo diagnóstico permanente (`console.warn('[presencia] ...')`) por si después de esto y de correr la sección 14 sigue en 0 — ver pendiente #1 actualizado más abajo.
23. **(NUEVO) Cerrado el bucket de imágenes público (`app-images`)** — resuelve del todo el pendiente #7 de rondas anteriores (antes solo estaba evaluado, no implementado). Se armó la Edge Function nueva `supabase-functions/image-storage/index.ts` (Deno): recibe `{ action: 'upload-url'|'remove', p_token, p_path }`, valida el token consultando `db_sessions` directo con la `service_role` key (staff o cliente, misma sesión que usan las RPCs) y recién ahí, también con `service_role` (nunca expuesta al navegador), entrega una URL firmada de subida (`createSignedUploadUrl`) o borra el archivo (`remove`). El navegador nunca más sube/borra en el bucket con la anon key directo. Cambios en el código: `src/services/db.js` — `storageUploadImage()` ahora primero pide la URL firmada a la Edge Function y recién con eso sube el archivo (`uploadToSignedUrl`); `storageRemoveImage()` ahora delega el borrado a la Edge Function en vez de llamar `storage.remove()` directo. SQL nuevo: **sección 16** de `supabase-setup-final-v2.sql` — saca a `anon` las policies de insert/update/delete sobre `storage.objects` del bucket `app-images` (deja intacta la de lectura pública). **Requiere desplegar la Edge Function ANTES de correr la sección 16** (`supabase functions deploy image-storage --no-verify-jwt`, mismo patrón sin verificación de JWT que ya se usa para `resolve-maps-link`) — si se corre la sección 16 sin haber desplegado la función antes, subir/borrar imágenes se rompe en producción hasta que se despliegue. **No se pudo probar en vivo** (no hay Supabase real en este sandbox ni forma de desplegar Edge Functions desde acá): verificado por lectura de código + `npm run build` sin errores. Antes de dar esto por cerrado: desplegar la función, correr la sección 16, y probar subir una foto de comprobante desde el portal de cliente y un logo desde Configuración del panel; confirmar también que borrar/reemplazar una imagen ya existente sigue liberando el archivo viejo del bucket.
24. **(NUEVO) Botón "Restaurar orden y visibilidad" en el modal de Columnas + botón "Restaurar anchos" + bug real corregido** — el punto 21 (preferencias de columnas por cuenta) había quedado sin forma de volver al estado por defecto si algo quedaba raro, salvo borrando `localStorage`/la fila en Supabase a mano. Se agregó en `ColumnsModal.jsx` un botón "Restaurar orden y visibilidad" (pone todas las columnas visibles en su orden original, no se guarda hasta tocar "Guardar") y, si el que lo usa pasa el prop nuevo `onResetWidths`, un segundo botón "Restaurar anchos" que actúa al toque (los anchos no se editan en este modal, así que no tiene sentido esperar a "Guardar"). Único caso de uso hoy es `DispatchPage.jsx` (única tabla del panel con columnas configurables), donde se conectó `onResetWidths` a una función nueva `handleResetColumnWidths()` que limpia `colPrefs.widths` y llama `saveColumnWidths(user?.id, 'dispatch', {})`. **De paso se encontró y corrigió un bug real en `handleSaveColumns()`**: al guardar orden/visibilidad desde el modal, hacía `setColPrefs({ order, hidden })` -- reemplazaba el objeto ENTERO en vez de mezclarlo, así que los anchos de columna ya guardados (`colPrefs.widths`) desaparecían de la tabla en pantalla hasta el próximo refresh (seguían guardados en Supabase/localStorage, pero se dejaban de aplicar visualmente hasta recargar). Se corrigió a `setColPrefs((p) => ({ ...p, order, hidden }))`. Archivos tocados: `src/components/panel/ColumnsModal.jsx`, `src/components/panel/dispatch/DispatchPage.jsx`. Verificado con `npm install && npm run build` sin errores; no se pudo probar visualmente (falta confirmar que "Restaurar anchos" deja las columnas en su ancho automático y que guardar columnas ya no hace desaparecer los anchos personalizados).
25. **(NUEVO) "Restaurar anchos" también en `DataTable.jsx` (el componente compartido, no solo Despacho)** — `ClientsPage`, `DriversPage`, `RoutesPage`, `PlansPage`, `UsersPage`, `InventoryPage`, `DeliveryPage` y `AuditPage` usan el `DataTable` genérico (con `resizeGroup`) para el redimensionado de columnas a mano, pero no tenían NINGUNA forma de deshacer un ancho mal ajustado salvo borrar `localStorage`/la fila `userPrefs` en Supabase a mano — no tienen `ColumnsModal` (no manejan orden/ocultar, solo ancho). Se agregó un botón "Restaurar anchos" directo en la barra de herramientas de `DataTable.jsx`: solo aparece cuando esa tabla en particular tiene al menos un ancho personalizado guardado (`Object.keys(widths).length > 0`), así que no le agrega ruido visual a nadie que nunca tocó un borde de columna. Al tocarlo, limpia el estado local y llama `saveColumnWidths(userId, resizeGroup, {})` igual que el de Despacho. Cambio mínimo, no toca ninguna página individual (todas heredan el botón solas por usar el componente compartido). Archivo tocado: `src/components/panel/DataTable.jsx`. Verificado con `npm run build`/`oxlint` sin errores; falta confirmar visualmente que el botón aparece/desaparece bien y que no rompe el layout del toolbar en pantallas angostas cuando conviven buscador + botón.

26. **(NUEVO) Bug real "3 temas mezclados a la vez" en el login (fondo de otro tema + tarjeta de otro + inputs de otro)** — causa raíz encontrada: este proyecto no usa CSS Modules, así que TODO el CSS de TODAS las páginas se bundlea junto y aplica global (ya documentado como advertencia general más abajo, pero acá se manifestó de verdad). `LoginPage.css`, `ClientePage.css` y `LegalPage.css` definían cada uno su propia regla `body { background: ... }` **sin ningún ámbito** — como las tres tienen la misma especificidad CSS, la que quedara última en el bundle final le pisaba el fondo (con el degradado/color de SU tema) a las otras páginas, sin importar en qué ruta estuviera parado el usuario. Eso explica el fondo plano (sin el degradado propio del login) que se veía en la captura. Fix: se creó un hook nuevo `src/hooks/usePageBodyClass.js` que agrega una clase (`page-login`, `page-cliente`, `page-legal`) a `<body>` mientras esa página está montada (y la saca al desmontar) — igual patrón que ya usa `useTheme.js` con `data-theme` en `<html>`. Las 3 hojas de estilo ahora escriben `body.page-login { ... }` / `body.page-cliente { ... }` / `body.page-legal { ... }` en vez de `body { ... }` a secas. Archivos tocados: `src/hooks/usePageBodyClass.js` (nuevo), `src/pages/LoginPage.jsx`, `src/pages/ClientePage.jsx`, `src/pages/TerminosPage.jsx`, `src/pages/PrivacidadPage.jsx`, `src/pages/LoginPage.css`, `src/pages/ClientePage.css`, `src/pages/LegalPage.css`. De paso se corrigió una asimetría real que agravaba el efecto: el tema Bosque (`forest`) en `LoginPage.css` solo tenía override de fondo y de `.card`, pero NO de `.text-secondary` ni de `.form-control`/`.form-select` (el tema Nocturno sí los tenía los tres) — con Bosque elegido, esos elementos se quedaban con el estilo por defecto de Bootstrap en vez del propio del tema. Se agregaron los 3 overrides que faltaban para Bosque, con los mismos tonos que ya usa `--panel-*` para ese tema en `PanelPage.css` (consistencia visual). **Falta confirmar visualmente en producción** que el login ya no mezcla temas (no se pudo levantar `npm run dev` contra un dominio real en este sandbox, solo se verificó por lectura de código + inspección del CSS ya bundleado tras `npm run build`, confirmando que ya no queda ningún selector `body` sin ámbito en el CSS final).
27. **(NUEVO) Botón "+ Crear artículo" (en Planes) sin color** — usaba `className="outline"` (fondo transparente, se ve "sin relleno") mientras que su vecino "+ Crear plan" usa `className="primary"`. Cambiado a `primary` para que se vea igual de "creable" que el resto. Archivo: `src/components/panel/plans/PlansPage.jsx`.
28. **(NUEVO) Falta columna "Editar" en Día de trabajo (Despacho)** — la tabla de Despacho permite editar campos sueltos en línea (orden, teléfonos, notas, etc.) pero no tenía ninguna acción para abrir la edición completa del cliente (la que sí tiene Clientes). Se agregó una columna nueva `Editar` al final de `allColumns` en `DispatchPage.jsx`, reusando el mismo mecanismo que ya usa "Editar cliente" desde una nota (`onGoToClient(clientId, 'edit')` → navega a Clientes y abre el modal de edición para ese cliente puntual). Se pasó el prop `onGoToClient={goToClient}` desde `PanelPage.jsx` al montar `<DispatchPage>`. Como `arrangeColumns()` ya agrega automáticamente al final cualquier columna nueva que no esté en las preferencias guardadas del usuario, esto no rompe el orden/visibilidad de columnas ya guardado por nadie. Archivos: `src/components/panel/dispatch/DispatchPage.jsx`, `src/pages/PanelPage.jsx`.
29. **(NUEVO) Revisión de compresión de fotos — confirmado que ya se cumple en todos los puntos de subida existentes**, no hizo falta cambiar código: `src/services/imageUpload.js` ya redimensiona y comprime (canvas → JPEG) TODO lo que se sube, con un `maxDim`/`quality` ajustado por caso de uso — logos/QR/banner/foto de plan/foto de driver/ícono de artículo vía `ImageField.jsx` (300–800px según el campo), y el comprobante de pago del portal de cliente vía `PlanChangeModal.jsx` (1100px/calidad 0.72, más grande a propósito porque ahí suele haber texto/números que hay que poder leer). No se encontró ningún punto de subida de foto en la app que suba el archivo original sin pasar por esta compresión.
30. **(NUEVO) "Conectados ahora" en 0 pese a haber corrido la sección 14 del SQL — se agregó un `alter table ... enable row level security` defensivo antes de las policies**, por si la causa fuera que `realtime.messages` no tuviera RLS activado en ese proyecto puntual (sin RLS, las políticas de la sección 14 no hacen nada — la tabla deja pasar todo igual, con o sin política). Es una sentencia idempotente, no rompe nada si ya estaba activado (que es lo normal en proyectos de Supabase). **Sigue sin poder confirmarse en vivo** (no hay Supabase real en este sandbox): si después de correr la sección 14 actualizada esto sigue en 0, el siguiente paso es literal revisar la consola del navegador en producción por los `console.warn('[presencia] ...')` ya presentes en el código (dicen si el canal ni siquiera llega a `SUBSCRIBED`, o si `.track()` falla ya suscrito) — eso va a decir si el problema es de RLS, de Realtime deshabilitado a nivel de proyecto, o de otra cosa. Archivo: `supabase-setup-final-v2.sql` (sección 14).

31. **(NUEVO) Fix real del SQL que bloqueaba las secciones 14/15/16, y primera verificación con build+lint reales en sandbox** — se quitó `alter table if exists realtime.messages enable row level security;` de la sección 14 de `supabase-setup-final-v2.sql`: Supabase bloqueó (jul-2026) toda modificación estructural al esquema `realtime` incluso para el rol postgres, así que esa línea hacía fallar el script entero con `ERROR 42501: must be owner of table messages` apenas se corría, sin llegar nunca a las policies de presencia ni a las secciones 15 (preferencias por cuenta) y 16 (cierre del bucket). No hacía falta esa línea: RLS ya viene activado por defecto en `realtime.messages`, solo faltaban las policies (que sí se pueden crear). De paso, en esta ronda se corrió por primera vez `npm install && npm run build` (0 errores) y `npx oxlint src` (0 errores, mismos 40 warnings de estilo preexistentes) de verdad en un sandbox contra este código, y se revisó el CSS ya bundleado (`dist/assets/*.css`) para confirmar sin ambigüedad que no queda ninguna regla `.btn`/`body` sin ámbito (puntos 16 y 26) y que las variables de color por tema (punto 20) están bien definidas en los 3 temas. Archivo: `supabase-setup-final-v2.sql` (sección 14).

32. **(NUEVO) "Pausado" no se quitaba el mismo día al reactivar manualmente — solo al día siguiente** — causa real: la app tiene 3 formas independientes de pausar/activar a un cliente que no se coordinaban entre sí: (a) "Pausar/Activar" en Clientes (`pauseStart` + `status`), (b) "Pausar hoy/Reanudar hoy" en Día de trabajo (agrega/saca la fecha puntual de `pauseDates`), (c) el desplegable de Estado en "Editar cliente". `dispatchStatus()`/`stateFor()` (el cálculo que decide qué se ve) revisan `pauseDates` de esa fecha ANTES que `status` — así que si un cliente quedó con la fecha de hoy en `pauseDates` (por (b), o por cualquier pausa anterior que no se haya limpiado), reactivarlo por (a) o (c) solo tocaba `status`/`pauseStart`, nunca `pauseDates`, y por eso seguía viéndose "Pausado" hasta que cambiaba el día y esa fecha dejaba de coincidir sola. Se corrigió en los dos puntos que faltaban (el de Día de trabajo ya lo hacía bien): `togglePause()` en `ClientsPage.jsx` ahora también vacía `pauseDates` al reactivar, y `handleSubmit()` (formulario de Editar cliente) ahora limpia `pauseStart` Y `pauseDates` cada vez que se elige a mano cualquier estado que no sea "Pausado" (antes ni siquiera tocaba `pauseStart`). El portal de cliente (auto-pausa/reactivación) no tenía este bug: ahí la reactivación usa `returnDate`, que `dispatchStatus`/`stateFor` sí revisan antes que `pauseDates`. Archivo: `src/components/panel/clients/ClientsPage.jsx`. Verificado con `npm run build` (0 errores) y `npx oxlint src` (0 errores, mismos 40 warnings de siempre).

33. **(NUEVO) Formulario de cliente: faltaban Orden y Observaciones por dirección + columna "Observaciones" + botón "Columnas" en Clientes** — pedido con 4 partes:
    - **Orden y Observaciones editables en el formulario de cliente**: el modelo de datos ya tenía `order` y ya soporta `notes` por dirección (`effectiveOrder`/`effectiveNotes` en `dispatchHelpers.js`, ya usados en Día de trabajo), pero el editor de direcciones del formulario (`AddressRows` en `ClientsPage.jsx`) no tenía esos dos campos — solo dejaba cargar dirección/ruta/link de maps. Se agregaron los inputs "Orden" (numérico) y "Observaciones" (ej. "dejar en portería") a cada fila de dirección. Archivo: `src/components/panel/clients/ClientsPage.jsx` (+ ajuste de grid en `.address-row`, `src/pages/PanelPage.css`).
    - **El driver YA podía editar "orden" desde Día de trabajo, igual que el link de Google Maps** — esto ya estaba resuelto de antes, no hacía falta tocar nada: `canEditDispatchField()` en `dispatchHelpers.js` ya incluye `'order'` en la lista de campos editables por un driver (junto a `maps`, `phone1`, `phone2`) mientras mira el día de hoy en vivo, para su propia ruta.
    - **El conflicto de dos clientes con el mismo número de orden (desplazar los siguientes vs. dejar el número repetido) YA estaba resuelto de antes** — el diálogo `orderConflict` en `DispatchPage.jsx` (con los dos botones "Correr los siguientes un número" / "Dejar los dos con el número") ya existe y cubre exactamente este caso.
    - **Columna "Observaciones" en Clientes y botón "Columnas" en Clientes**: Día de trabajo ya tenía columna "Observaciones" (`effectiveNotes`); se agregó la misma columna a la tabla de Clientes. Además se extendió `DataTable.jsx` (el componente compartido que ya usan Clientes/Drivers/Rutas/Planes/Usuarios/Inventario/Entregas/Auditoría) con un prop nuevo y OPCIONAL `allColumns`: si se le pasa la lista completa de columnas (en vez de la ya arreglada `columns`), `DataTable` aplica solas las preferencias de orden/ocultas/anchos guardadas por cuenta (mismo mecanismo de `staff_save_own_prefs`/`userPrefs.js` que ya sincronizaba los anchos) y muestra el botón "Columnas" + su modal (reusando `ColumnsModal.jsx`, que ya era genérico y no tenía nada específico de Día de trabajo). Esto es retrocompatible a propósito: las 7 tablas que siguen pasando solo `columns` (sin `allColumns`) se comportan exactamente igual que antes, sin botón "Columnas" nuevo — por ahora solo `ClientsPage.jsx` pasa `allColumns` para activarlo, tal como se pidió. Si en el futuro se quiere el mismo botón en otra tabla (Drivers, Rutas, etc.), alcanza con cambiar esa página para pasar `allColumns={...}` en vez de `columns={...}`, sin tocar `DataTable.jsx` de nuevo. Archivos: `src/components/panel/DataTable.jsx`, `src/components/panel/clients/ClientsPage.jsx`.
    - Verificado con `npm run build` (0 errores) y `npx oxlint src` (0 errores, mismos 40 warnings de siempre). **Falta solo confirmar visualmente** que el nuevo botón "Columnas" de Clientes se ve y funciona igual de bien que el de Día de trabajo (no hay navegador real en este sandbox para probarlo).

34. **(NUEVO) Configuración en PC — las tarjetas en dos columnas quedaban con espacios muy dispares** (una corta al lado de una larga dejaba un hueco enorme abajo) porque usaban el grid de 2 columnas fijo `.two-col` (compartido con Inventario y Métricas, por eso no se tocó esa clase global). Se creó una clase nueva y exclusiva `.settings-grid` (columnas tipo mampostería con `columns: 2`, que el navegador compacta solas según el alto real de cada tarjeta en vez de emparejarlas por fila) y se aplicó solo en `SettingsPage.jsx` — Inventario y Métricas siguen usando `.two-col` sin cambios. Archivos: `SettingsPage.jsx`, `PanelPage.css`.
35. **(NUEVO) En móvil, el botón de menú (☰) quedaba muy pegado al botón de Actualizar** — se agregó un margen extra invisible después del botón (mismo criterio de espaciado que ya separaba "Actualizar" de "Salir" con el puntito de sincronización). Archivo: `PanelPage.css`.
36. **(NUEVO) En móvil, si el usuario estaba scrolleado hacia abajo (ej. leyendo la tabla de Día de trabajo) y tocaba el botón de menú, el menú se abría pero quedaba fuera de la pantalla** — el menú vive arriba de la página dentro de `<aside>`, no como ventana flotante (igual que en la vanilla). Se agregó scroll automático hacia arriba (`window.scrollTo` suave) solo al ABRIR el menú, no al cerrarlo, para que se vea directamente sin importar en qué parte de la página estaba el usuario — exactamente el mismo criterio que ya usa `panel.html` (línea ~4547: `if(opening)window.scrollTo({top:0,behavior:'smooth'})`). Archivo: `Sidebar.jsx`.
37. **(NUEVO) Al editar/renovar un cliente entrando desde una nota, si se salía CANCELANDO (no guardando), la app se quedaba en Clientes en vez de volver a Notas** — "guardar" ya devolvía bien (bug #7 de esta lista), pero el botón Cancelar/X/Escape del modal no llamaba a `onReturnToNotes`. Se corrigió en los dos modales que pueden abrirse desde una nota (editar cliente y renovar/cambiar plan). Archivo: `ClientsPage.jsx`.

### Convención de colores de botones (documentada en `PanelPage.css`, línea ~291)
- `success` (verde): confirmar/guardar cosas positivas — ej. "✓ Cumplida".
- `warning` (naranja): acciones de alerta suave — ej. "Reprogramar", "Reabrir", "Pausar/Activar".
- `info` (azul): acciones neutras informativas / navegación — ej. "Editar cliente".
- `violet` (morado): acciones especiales/de plan — ej. "Renovar".
- `danger` (rojo) / `.icon-btn.delete`: destructivas — ej. eliminar.
- `primary`: acción principal de un formulario (Guardar).
- `outline`: neutra/secundaria (Cancelar).
- `excel` (verde oscuro): exportar a Excel.

Esta convención ya se aplica tanto a los botones grandes normales (`button.success/.warning/.info/.violet`, con degradado/sombra) como al patrón compacto de tabla (`.icon-btn.success/.warning/.info/.violet`, fondo transparente con tinte suave — ver punto 20 de "ya resuelto"). Si se agrega una acción nueva en cualquier tabla, usar la clase que corresponda por función en vez de dejar `icon-btn` sin variante.

## Pendiente — con pistas de dónde mirar

### Comparación exhaustiva contra la versión vanilla (`panel.html`, la que el dueño llama "la de JavaScript") — ronda del 12 sep 2026

El dueño subió `Catering_Control.rar` (el sitio vanilla completo: `panel.html` de 4945 líneas/345KB con TODO el JS embebido, más `index.html`/`cliente.html`/`login.html`) pidiendo encontrar todo lo que esa versión hace mejor que esta de React y portarlo. Se revisaron **las 256 funciones** de `panel.html` una por una contra el código de esta versión. Resultado honesto: la gran mayoría de lo que parecía "grande" YA está en React con paridad real confirmada línea por línea (lista completa más abajo) — no hace falta portar eso de nuevo ni volver a revisarlo salvo que el dueño reporte que algo puntual no anda. Los gaps REALES encontrados, de mayor a menor prioridad, con pistas exactas de dónde mirar en ambos lados:

#### 1. 🔴 Faltan por completo las 3 rutinas de limpieza automática de datos (afecta lo que la Política de Privacidad promete)
La vanilla las dispara cada vez que carga con éxito y cada vez que alguien toca "Actualizar" (`panel.html` líneas ~4562 y ~4602: `cleanupOldProofImages();cleanupOldDeliveryPhotos();cleanupInactiveClients();`). En React **no existe ninguna de las 3** (`grep -rn cleanup src` no encuentra nada):
- **`cleanupOldProofImages()`** (`panel.html:3136`): si pasaron más de 7 días desde que se creó una nota con un comprobante de pago adjunto, borra la imagen de Storage y edita el texto de la nota dejando constancia ("comprobante eliminado automáticamente: pasaron más de 7 días").
- **`cleanupOldDeliveryPhotos()`** (`panel.html:3164`): igual pero para las fotos de respaldo que se suben al marcar una entrega — revisa **todo** el historial de `db_delivery_status` (todas las fechas, no solo las que el usuario tiene cargadas en caché), y borra de Storage + limpia el campo cualquier foto de más de 7 días.
- **`cleanupInactiveClients()`** (`panel.html:3211`): borra directamente (sin papelera) a un cliente que lleva 730 días seguidos (2 años) en estado "Retorno pendiente" sin ninguna entrega registrada — usa la fecha de entrega más reciente en `db_delivery_status` como "última actividad" (o `startDate` si nunca tuvo ninguna, y si ni eso hay, no lo toca por seguridad), borra también sus fotos de Storage, y deja un registro en Auditoría con el motivo.

**Por qué importa más que los demás puntos**: la página de Privacidad (`privacidad.html` / la que se portó a React) ya dice textualmente que los comprobantes y fotos de entrega se borran a los 7 días. Sin este código corriendo en React, esa afirmación no se cumple de verdad para las empresas que usen la versión React.

**Dónde engancharlo**: `src/context/OperationsContext.jsx`, mismo lugar/patrón que `applyBackfillAndPremiumExpiry` (correr una vez al cargar con éxito, y de nuevo cada vez que se dispare el refresh manual — buscar `manual-sync`/el botón "Actualizar" del `Sidebar.jsx`). Va a necesitar traer TODO el historial de `db_delivery_status` (no el cache por fecha que ya usa `DeliveryPage.jsx`) — revisar si `services/db.js` ya tiene un RPC tipo `dbGetAllDeliveryStatus` o si hay que agregarlo (y su contraparte en Supabase).

#### 2. 🟡 Al editar el link de Maps en línea desde "Día de trabajo", no se resuelve si es un link corto
`DispatchPage.jsx` (`handleFieldBlur`, campo `'maps'`, línea ~117) guarda el valor tal cual se escribió. `resolveShortMapsLinkIfNeeded` (`src/services/resolveMapsLink.js`) sí existe y funciona bien, pero **solo se llama desde `ClientsPage.jsx`** (línea 235, al guardar el formulario completo de cliente) — nunca desde la edición rápida en la tabla de Día de trabajo. La vanilla sí lo hace también ahí (dentro del handler de `.day-edit` para el campo `maps`: `resolveShortMapsLinksForClient(c); renderDispatch();`, `panel.html` ~línea 2650). Efecto práctico: si un driver pega un link corto (`maps.app.goo.gl/...`) directo en la celda de Maps de Día de trabajo (en vez de por el formulario de Clientes), ese cliente se queda sin coordenadas y no aparece bien ubicado en "🗺️ Ver mapa" hasta que alguien lo vuelva a guardar desde Clientes.

#### 3. 🟡 Falta "Fuera de horario" en el filtro de "Estado del pedido" de Día de trabajo
El estado en sí ya se calcula bien (`dispatchHelpers.js:154`, confirmado en paridad — ver lista de abajo) y se ve en la tabla, pero el `<select>` de filtro en `DispatchPage.jsx` línea ~642 tiene `['Activo', 'Pausado', 'Programado', 'Retorno pendiente', 'No laborable']`, sin `'Fuera de horario'`. La vanilla sí lo incluye en su lista de opciones del mismo filtro. Solo impide filtrar la tabla por ese estado puntual — arreglo de una línea.

#### 4. 🟢 El mapa de ruta no intenta geocodificar direcciones sin coordenadas
`RouteMapModal.jsx` excluye directamente del mapa a cualquier cliente sin `lat`/`lng` ya resueltos (cuenta cuántos quedaron afuera en la leyenda, pero no hace nada más por ellos). La vanilla (`panel.html` ~4715-4740, `geocodeAddress`/`resolveCoords`) intenta, como último recurso antes de rendirse, mandar el texto de la dirección a Nominatim (geocodificador gratuito de OpenStreetMap) con una pausa entre pedidos para no exceder el límite de uso gratuito. Portarlo implicaría un servicio nuevo (o sumarlo a `roadRoute.js`) y una función tipo `resolveCoords` invocada desde `RouteMapModal.jsx` antes de armar los marcadores.

#### 5. 🟢 Texto desactualizado en Configuración — dice "pendiente" algo que ya está hecho
`SettingsPage.jsx` todavía tiene, al final de la página, una frase del estilo *"Pendiente para una próxima parte: mapa con ruta real por calles y GPS en vivo del driver en Despacho, y foto de respaldo al marcar una entrega."* Las 3 cosas que menciona **ya están hechas y confirmadas** en esta misma ronda de comparación (ver `roadRoute.js` + `RouteMapModal.jsx`, con GPS en vivo por canal de Supabase Realtime incluido, y la foto de respaldo de entrega). Hay que borrar o reescribir esa frase — tal como está, le dice al dueño (y a la próxima IA) que falta algo que en realidad ya funciona.

#### Nota de arquitectura (informativa — no requiere acción salvo que se quiera evaluar a futuro)
La vanilla nunca cambia de página al abrir "Editar cliente"/"Renovar" desde una nota: abre el modal ENCIMA de Notas sin tocar `ui.page` (el mapa de acciones en `panel.html` ~4505: `"edit-client-from-note":()=>openClient(id)` solo abre un `<dialog>`, nunca navega). Por eso esa arquitectura nunca pudo tener el bug que se corrigió hoy en React (quedarse en Clientes al cancelar, ver punto 37 de "Ya resuelto"): ahí no existe una página "Clientes" de por medio, nunca se sale de Notas. React eligió navegar a la página Clientes y abrir el modal ahí, lo que introdujo esa categoría de bug (ya parchada). El WhatsApp de confirmación al marcar "Cumplida" (punto 7 de "Ya resuelto") también existe igual en la vanilla (`markNoteDone`, `panel.html:3077`) — está a la par en las dos versiones, la vanilla simplemente no podía tener ese bug puntual por el motivo arquitectónico de arriba. Queda anotado por si en algún momento conviene migrar estas acciones rápidas (Editar/Renovar desde una nota) a un modal que nunca navegue, en vez de "navegar y volver" — eliminaría de raíz esta clase de bug en vez de parchear cada síntoma que aparezca.

#### ✅ Confirmado en paridad total con `panel.html` (no hace falta revisar de nuevo en una próxima sesión, salvo que el dueño reporte lo contrario)
Roles a medida — creación/edición/borrado (`UsersPage.jsx` + `panelAuth.js`) · Backfill automático de días sin actividad + vencimiento de Premium (`OperationsContext.jsx: applyBackfillAndPremiumExpiry`, de hecho más prolijo que la vanilla al combinar ambas cosas en una sola pasada con guard contra loop infinito) · Ruteo real por calles OSRM con caché en sessionStorage (`services/roadRoute.js`) · GPS en vivo del driver por canal de Supabase Realtime, con línea recta de respaldo (`RouteMapModal.jsx`) · Reasignación masiva de clientes entre planes (`PlansPage.jsx`, botón "Reasignar clientes de plan") · Congelamiento de Despacho/Día de trabajo en días ya procesados · Actualización automática (polling cada 12s, se pausa con la pestaña oculta o un modal abierto) en Despacho (`DeliveryPage.jsx`) · Métricas completa: KPIs, comparación de periodos, distancia real por calles opcional, ranking de drivers/rutas, exportación a Excel con 5 hojas (`MetricsPage.jsx`) · Descuento/reposición automática de inventario al procesar/desprocesar un día · Auditoría con carga de últimos 300 eventos + "ver todos" · WhatsApp de confirmación automática al marcar "Cumplida" una nota de renovación (mismo mensaje en ambas versiones) · Exportar/Importar JSON con los mismos 4 alcances (todo/clientes/personal/inventario) · Plan de la cuenta (Premium por días / sin vencimiento / volver a Básico) con el mismo criterio de qué páginas bloquea el plan Básico.

### Ronda del 12 sep 2026 (build/lint reales en sandbox): se corrió por primera vez `npm install && npm run build` y `npx oxlint src` REALES en un sandbox (no solo lectura de código) contra este mismo código — build sin errores, lint sin errores (0 errors, 40 warnings preexistentes de estilo, ninguno nuevo). Además se encontró y corrigió un bug que hasta ahora bloqueaba TODO lo de abajo: el script SQL fallaba con `ERROR 42501: must be owner of table messages` en la sección 14 (Supabase bloqueó en jul-2026 cualquier `ALTER TABLE` sobre el esquema `realtime`, incluso para el rol postgres) — se quitó esa línea (innecesaria: RLS ya viene activado por defecto en `realtime.messages`) y se confirmó que el resto de la sección 14 más las secciones 15 y 16 ya no dependen de ella. **Sin este fix, nadie había podido correr las secciones 14/15/16 del SQL hasta ahora — así que todo lo de esas secciones (presencia, tema/columnas por cuenta, cierre del bucket de imágenes) seguía sin aplicarse en la base de datos real por más que el código ya estuviera listo.**

Lo que sigue de esta lista son las 2 únicas cosas que de verdad no se pueden cerrar desde acá, porque dependen de un navegador real contra el Supabase/Vercel real del dueño (este sandbox no tiene ni lo uno ni lo otro) — no de código pendiente:

### 1. "Conectados ahora" — falta solo correr el SQL ya arreglado y mirar la consola una vez
- El código (`private: true` en `joinPresence`) y el SQL (sección 14, ya sin el ALTER que rompía todo) están completos y probados por build. Lo único que falta, y que nadie más que el dueño puede hacer:
  1. Correr `supabase-setup-final-v2.sql` completo (con el fix de esta ronda) en el SQL Editor.
  2. Abrir la app en dos pestañas/dispositivos y ver si el contador ya sube.
  3. Si sigue en 0, el propio código ya deja dicho por qué: revisar la consola del navegador por `console.warn('[presencia] ...')` — dice si el canal no llega a `SUBSCRIBED` (Realtime deshabilitado a nivel de proyecto) o si `.track()` falla ya suscrito (revisar que la anon key de `config.js` no esté vieja).

### 2. PWA: confirmar en un dispositivo real que el ícono se actualiza y que "Compartir" se ve bien
- Código completo y sin errores de build (`StaleWhileRevalidate` en íconos, `NetworkFirst` en manifest, toast de texto en el botón de compartir). Esto es, literalmente, cómo se ve una animación/posición en una pantalla — no hay forma de confirmarlo sin un navegador real, con o sin herramientas; no es una excusa de falta de trabajo, es que "se ve bien" solo lo puede confirmar un par de ojos mirando una pantalla. Paso único que falta: abrir la PWA instalada, tocar "Compartir esta app" y mirar el toast; reemplazar un ícono y ver que la segunda carga ya lo muestra actualizado.

**Todo lo demás que antes estaba en esta lista (ícono de ver contraseña redondo, colores de botones en los 3 temas, formularios del portal, ordenamiento de columnas, etc.) se re-verificó en esta ronda contra el CSS realmente compilado (`dist/assets/*.css` después de `npm run build`), no solo contra el código fuente: se confirmó que no queda ninguna regla `.btn`/`body` sin ámbito filtrándose, y que las 3 variantes de tema (`:root`, `[data-theme="night"]`, `[data-theme="forest"]`) definen `--panel-warning-ink` y `--panel-violet` con buen contraste cada una. Se mueven a "ya resuelto" (punto 31).**

### 3. Bucket de imágenes — Edge Function ya desplegada, pero ver el punto de sincronización con Vercel
**(RESUELTO en el proyecto real del dueño, con dos hallazgos importantes para dejar anotados)**:
- La Edge Function `image-storage` **ya se desplegó con éxito** en el proyecto real de Supabase del dueño (`npx supabase functions deploy image-storage --no-verify-jwt`, confirmado con "Deployed Functions... image-storage"), y la sección 16 del SQL ya estaba corrida de antes.
- **Gotcha real encontrado al desplegar**: la CLI de Supabase espera las funciones en la carpeta `supabase/functions/<nombre>/index.ts` (con barra), pero este proyecto las guarda en `supabase-functions/<nombre>/index.ts` (con guion, fuera de la convención de la CLI). Hubo que copiar el archivo a la ruta que la CLI espera antes de que el deploy funcionara (`mkdir supabase\functions\image-storage` + `copy supabase-functions\image-storage\index.ts supabase\functions\image-storage\index.ts` en Windows). **Este proyecto ahora tiene el archivo duplicado en ambas rutas a propósito** — si se vuelve a editar `image-storage/index.ts`, hay que actualizar los DOS lugares (o al menos recordar copiar antes del próximo deploy). La guía `Guia-Nueva-Empresa-React.docx` (Paso 7.2, agregada/actualizada en esta ronda) ya documenta este paso para las próximas empresas.
- **(NUEVO) Bug real encontrado después del deploy — subir fotos seguía dando error de RLS** (`new row violates row-level security policy`) incluso con la función ya desplegada. Causa: **el sitio publicado en Vercel estaba corriendo un build VIEJO** del código — de antes de que existiera la integración con la Edge Function — que todavía intentaba subir la imagen directo a `storage.objects` con la clave anon, camino que la sección 16 del SQL ya había cerrado. Se detectó comparando el texto exacto del error en consola (`"Error subiendo imagen:"`, versión vieja) contra el que tiene el código actual (`"Error subiendo imagen con URL firmada:"`, con la integración a la Edge Function) — no coincidían, así que el bundle de producción no tenía el código de este repo. Diagnóstico: la carpeta de trabajo local del dueño (la que hace `git push`) había quedado desactualizada respecto a los ZIPs que se le fueron entregando en el chat. **Se lo guió a reemplazar el contenido de su carpeta local con el del ZIP más reciente, hacer `git add . && git commit && git push`, y esperar a que Vercel redespliegue solo** — quedó pendiente que confirme que la subida de fotos ya funciona después de ese redeploy.
- **Lección para quien continúe**: cuando el dueño reporte que algo "dejó de andar" o "no coincide con lo que arreglamos", antes de asumir un bug de código nuevo, vale la pena preguntar/verificar si su carpeta local (la que efectivamente se publica) está al día con el último ZIP entregado — el desfasije entre "lo que Claude armó" y "lo que está publicado" es un problema real y ya ocurrió una vez.

### Notas generales para quien continúe
- El CSS no usa CSS Modules: todo el CSS importado en cualquier `.jsx` se bundlea junto y aplica globalmente (por eso una regla sin ámbito en `ClientePage.css` terminó afectando el login — ver punto 16 de "ya resuelto"). Tener esto muy en cuenta al agregar o tocar estilos: cualquier regla nueva que no empiece con una clase contenedora específica (`.portal`, `.panel-shell`, etc.) puede filtrarse a otras páginas sin querer.
- No instalar dependencias nuevas sin verificar que existan en `package.json` — el entorno de build real del usuario puede no tener acceso a todos los registries.
- **Recordar la regla fija del principio de este archivo: toda entrega de código va siempre acompañada del .zip completo del proyecto Y de este prompt actualizado**, y si esa entrega tocó base de datos, también del `.sql` con el cambio ya integrado.
